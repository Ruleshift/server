# Ruleshift website

The GitHub Pages site is a Vite/React static build. It uses query-string
navigation for the documentation view so it works without server rewrites.

```powershell
npm install
npm run dev
npm run build
```

The Pages workflow builds `site/` and deploys `site/dist/`.
