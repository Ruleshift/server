apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ruleshift-core

resources:
  - deployment.yaml

# Apply secret.example.yaml (with real values) and ingress.example.yaml separately.
