global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: ruleshift
    metrics_path: /metrics
    static_configs:
      - targets: [ruleshift-operations:9091]
